setlocale(LC_ALL,"Russian");
int n, i, j, L,m,p,f;
	cout << "Введите n: ";
	cin >> n;
	int* mass = new int[n];
	int* mass2 = new int[n];
	for (i = 0; i < n; i++)
	{
		cin >> mass[i];
	}
	cout << "Введите L:";
	cin >> L;
	m = n;
	p = 1;
	f= 0;
	if (L > 0) {
		for (i = 1; i < n; i++)
		{
			if (mass[i - 1] == mass[i]) {
				p = p + 1;

			}
			else if (p > L)
			{
				mass2[f] = 0;
				f = f + 1;
				m = m - p + 1;
				p = 1;
			}
			else
			{
				for (j = f; j < p + f; j++) {
					mass2[j] = mass[i - 1];
				}
				f = f + p;
				p = 1;
			}
			if (i == n - 1)
			{
				if (p > L)
				{
					mass2[f] = 0;
					f = f + 1;
					m = m - p + 1;
					p = 1;

				}
				else
				{
					for (j = f; j < p + f; j++) {
						mass2[j] = mass[i];
					}
					f = f + p;
					p = 1;
				}
			}
		}

			cout << "Преобразованный массив:" << endl;
			for (i = 0; i < m; i++)
			{
				cout << i + 1 << ": " << mass2[i] << endl;
			}
		
	}
	else cout << "Error";
  return 0;
