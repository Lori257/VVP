setlocale(LC_ALL, "Rus");	
	int n,k,i;
	cout << "Введите n: ";
	cin >> n;
	int* A = new int[n];
	int* B = new int[n];
	for (i = 0; i < n; i++) {
		cin >> A[i];
	}
	for (i = 0; i < n; i++) {
		cin >> B[i];
	}
	for (i = 0; i < n; i++) {
		k = A[i];
		A[i] = B[i];
		B[i] = k;
	}
	cout << "Массив A: ";
	for (i = 0; i < n; i++) {
		cout << A[i]<<" ";
	}
	cout << "Массив B: ";
	for (i = 0; i < n; i++) {
		cout << B[i] << " ";

	}
  return 0;
