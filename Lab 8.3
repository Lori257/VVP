setlocale(LC_ALL, "Russian");
	double a, b, c, a1, b1, c1, ab, ac;
	cout << "Введите координаты точки a: ";
	cin >> a >> a1;
	cout << "Введите координаты точки b: ";
	cin >> b >> b1;
	cout << "Введите координаты точки c: ";
	cin >> c >> c1;
	ab = sqrt(((b - a) * (b - a)) + ((b1 - a1) * (b1 - a1)));
	ac = sqrt(((c - a) * (c - a)) + ((c1 - a1) * (c1 - a1)));
	if (ab > ac) {
		cout << "Ближайшая точка - С(" << c << "; " << c1 << ")" << endl << "Расстояние до А: " << ac;
	}
	else if (ab < ac) {
		cout << "Ближайшая точка - B(" << b << "; " << b1 << ")" << endl << "Расстояние до А: " << ab;
	}
	else cout << "Точки В и С равноудалены от точки А. Расстояние: " << ac;
	return 0;
