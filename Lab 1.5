setlocale(LC_ALL, "Russian");
	double a, b;
	cout << "Введите a и b: ";
	cin >> a >> b;
	if (a != 0 && b != 0) {
		cout << "Сумма: " << abs(a + b) << endl;
		cout << "Произведение: " << abs(a*b)<<endl;
		cout << "Частное: " << abs(a / b)<<endl;
		cout << "Разность: " << abs(a - b)<<endl;
	
	}
	else cout << "Error";
	return 0;
