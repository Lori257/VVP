setlocale(LC_ALL, "Russian");
	int n,min;
	cout << "Введите n: ";
	cin >> n;
	int* A = new int[n];
	for (int i = 0; i < n; i++) {
		cin >> A[i];
	}
	min = A[2];
	for (int i = 2; i < n; i+=2) {
		if (A[i] < min) {
			min = A[i];
		}
	}
	cout << "Минимальный элемент с четным номером: " << min;
	return 0;   
