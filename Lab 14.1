setlocale(LC_ALL, "Russian");
	int k, l, n;
	float f;
	f = 0;
	cout << "Введите n: ";
	cin >> n;
	int* mass = new int[n];
	cout << "Введите k и l: ";
	cin >> k >> l;
	if (k >= 1 && k <= l && l <= n) {
		for (int i = 0; i < n; i++) {
			cin >> mass[i];
		}
		for (int i = k; i <= l; i++) {
			f = mass[i] + f;
		}
		cout << "Среднее арифметическое элементов: " << f / (l - k + 1);
	}
	else cout << "Error";
	return 0;
