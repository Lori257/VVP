setlocale(LC_ALL, "Russian");
	int a, b, c;
	cout << "Введите А и В: ";
	cin >> a >> b;
	if (a > b)
	{
		c = a % b;
		cout << endl << "Незанятая часть А: " << c;
	}
	else
	{
		cout << endl << "Error";
	}
