setlocale(LC_ALL, "Rus");
	int n, i, k;
	cout << "Введите n: ";
	cin >> n;
	k = 0;
	int* mass = new int[n];
	for (i = 0; i < n; i++) {
		cin >> mass[i];
	}
	for (i = 0; i < n-1; i++) {
		if (mass[i] > mass[i + 1]) {
			k = mass[i];
			mass[i] = mass[i+1];
			mass[i+1] = k;
		}
	}
	cout << "Новый массив: ";
	for (i = 0; i < n; i++) {
		cout << mass[i] << " ";
	}
  return 0;
