setlocale(LC_ALL, "Rus");
		int i,n,k;
		cout << "Введите n: ";
		cin >> n;
		int* mass = new int[n];
		k = 0;
			for (i = 0; i < n; i++) {
				cin >> mass[i];
		}
			for (i = 0; i < n; i++) {
				if (mass[i] % 2 != 0) {
					k = mass[i];
				}
			}
			for (i = 0; i < n; i++) {
				if (mass[i] % 2 != 0) {
					mass[i] = mass[i] + k;
				}
			}
			cout << "Новый массив: ";
			for (i = 0; i < n; i++) {
				cout << mass[i]<<" ";
			}
      return 0;
