setlocale(LC_ALL, "Russian");
    int n, a, b,f;
	cout << "Введите n,a и b: ";
	cin >> n >> a >> b;  	
	int* mass = new int[n];
	mass[0] = a;
	mass[1] = b;
	if (n > 2) {
		for (int i = 2; i < n; i++) {
			f = 0;
			for (int j = 0; j < i; j++) {
				f = f + mass[j];
			}
			mass[i] = f;			
		}
		for (int i = 0; i < n; i++) {
			cout << mass[i] << " ";
		}
	}
	else cout << "Error";
	return 0;   
