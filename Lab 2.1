setlocale(LC_ALL, "Russian");
	double x1, y1, x2, y2,d;
	cout << "Введите координаты точек: ";
	cin >> x1 >> y1 >> x2 >> y2;
	d = sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2));
    cout << "Расстояние между двумя точками: "<<d;
	return 0;
