setlocale(LC_ALL, "Russian");
	int n,a,d;
	cout << "Введите n, a и d: ";
	cin >> n >> a >> d;
	int*mass = new int[n];
	if (n > 1) {
		for (int i = 0; i < n; i++) {
			mass[i] = a * pow(d, i);
			cout << mass[i] << " ";

		}

	}
	else cout << "Error";
	return 0;
