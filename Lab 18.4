setlocale(LC_ALL, "Russian");
	int m, n, i, j,s;
	cout << "Введите m и n: ";
	cin >> m >> n;
	s = 0;
	int** mass = new int* [n];
	int* mass2 = new int[m];
	for (i = 0; i < n; i++) {
		mass[i] = new int[m];
	}
	cout << "Введите элементы матрицы:" << endl;
	for (i = 0; i < n; i++) {
		for (j = 0; j < m; j++)
		{
			cin >> mass[i][j];
			mass2[j] = 0;
		}
	}
	for (j = 0; j < m; j++)
	{
		for (i = 0; i < n; i++) {
			s=s+mass[i][j];
		}
		for (i = 0; i < n; i++)
		{
			if (mass[i][j] > s / n) {
				mass2[j]=mass2[j]+1;
			}
		}
		s = 0;
	}
	for (i = 0; i < m; i++) {
		cout << " Cтолбец: " <<i+1<< " ";
		cout << "Элементов больше среднего арифметического по строке: " << mass2[i]<<endl;
	}
	return 0;
