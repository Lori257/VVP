setlocale(LC_ALL, "Russian");
	int n,max;
	cout << "Введите n: ";
	cin >> n;
	int* mass = new int[n];
	for (int i = 0; i < n; i++) {
		cin >> mass[i];
	}
	max = 0;
	for (int i = 1; i < n - 1; i++) {
		if (mass[i] > mass[i + 1] && mass[i] > mass[i - 1]) {
			max = i;
		}		
	}
	if (max !=0)	cout << "Последний локальный максимум: " << max;
	else cout << "Локальный максимум отсутсвует";
	return 0;
