setlocale(LC_ALL, "Russian");
	int n, k, tmp2, rnd,i,j;
	cout << "Введите n: ";
	cin >> n;
	rnd = 1;
	k = 0;
	int* mass = new int[n];
	int* mass2 = new int[n];
	int* mass3 = new int[n];
	cout << "Введите массив: ";
	for ( i = 0; i < n; i++)
	{
		cin >> mass[i];
	}
	for ( i = 0; i < n; i++)
	{
		k = 0;
		for ( j = 0; j < n; j++)
		{
			if (mass[i] == mass[j])
			{
				k=k+1;
			}
		}
		if (k == 2)
		{
			mass3[i] = mass[i];
		}
	}
	for ( i = 0; i < n; i++)
	{
		tmp2 = 1;
		for ( j = 0; j < n; j++)
		{
			if (mass[i] == mass3[j]) tmp2 = INT_MAX;
		}
		if (tmp2 == 1)
		{
			mass2[rnd] = mass[i];
			rnd=rnd+1;
		}
	}
	cout << endl;
	cout << "Новый массив: ";
	for ( i = 1; i < rnd; i++)
	{
		cout << mass2[i] << " ";
	}
	cout << "Размер массива: " << (rnd - 1);
	return 0;
