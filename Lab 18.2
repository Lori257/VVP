setlocale(LC_ALL, "Russian");
	int m, n, k, i, j, sum, pr;
	sum = 0;
	pr = 1;
	cout << "Введите m,n и k: ";
	cin >> m >> n >> k;

	int** A = new int* [m];
	for (i = 0; i < m; i++) {
		A[i] = new int[n];
	}
	cout << "Введите массив: " << endl;
	for (i = 0; i < m; i++) {
		for (j = 0; j < n; j++) {
			cin >> A[i][j];
		}
	}
	if (k >= 1 && k < m) {
		for (j = 0; j < n; j++) {
			pr = pr * A[k][j];
			sum = sum + A[k][j];
		}
		cout << "Сумма: " << sum << endl;
		cout << "Произведение: " << pr;
	}
	else cout << "Error";
	return 0;
