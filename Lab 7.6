setlocale(LC_ALL, "Russian");
	int a, b, c;
	cout << "Введите a,b,c: ";
	cin >> a >> b >> c;
	if (a * a == b * b + c * c || b * b == a * a + c * c || c * c == a * a + b * b) cout << endl << "True";
	else cout << endl << "False";
	return 0;
