#include "pch.h"
#include <iostream>
#include <Math.h>
#include <windows.h>
#include <string>

using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");	
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	int i;	
	char c;
	string s;
	s = "";	
	cout << "Введите строку S: ";	
	getline(cin, s);
	for (i = 0; i < s.length(); i++) {
		cout << s[i] << " ";
	}
	cout << endl;
}
