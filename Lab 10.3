setlocale(LC_ALL, "Russian");
    int n;
    double x, h;
    cout << "Введите n: ";
    cin >> n;
    if (n > 0) {
    x = 0;
    for (int i = 1; i <= (2 * n - 1); i = i + 2) {
     x = x + i;
     cout << x << " ";
      }
     cout << endl << "Квадрат " << n << " равен: " << x;

	}

	else cout << "Error";

	return 0;
