setlocale(LC_ALL, "Russian");
	int n;
	cout << "Введите n : ";
	cin >> n;
	int *mass = new int[n];
	cout << "Введите элементы массива :";
	for (int i = 0; i < n; i++)
	{
		cin >> mass[i];
	}
	for (int i = 0; i < n / 2; i++)
	{
		cout<< mass[i]<< " ";
		cout<< mass[n - i - 1] <<" ";
	}
	if (n % 2 != 0)
	{
		cout <<mass[n / 2];
	}
	return 0;
