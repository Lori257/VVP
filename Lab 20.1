#include "pch.h"
#include <iostream>
#include <Math.h>
#include <windows.h>
#include <string>

using namespace std;

int main()
{		
	setlocale(LC_ALL, "Russian");
	char c;	
	cout << "Type symbol C: ";
	cin >> c;
	cout << char(int(c) - 1) << " " << c << " " << char(int(c) + 1) << endl;
}
