setlocale(LC_ALL, "Russian");
	int n, p, h;
	cout << "Введите n: ";
	cin >> n;
	int* mass = new int[n];
	for (int i = 0; i < n; i++) {
		cin >> mass[i];
	}
	h = 0;
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			if (i != j && mass[i] == mass[j]) {
				h = 1;
			}
		}
	}
	if (h != 1) {
		for (int i = 0; i < n - 2; i++) {
			if (mass[i + 1] - mass[i] == mass[i + 2] - mass[i + 1]) {
				p = mass[i + 1] - mass[i];
			}
			else p = 0;

		}
		cout << "Ответ: " << p;
	}
	else cout << "Есть одинаковые числа";
	return 0;
