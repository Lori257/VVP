setlocale(LC_ALL,"Russian");
int a, i, sum,k;
cout << "Введите a: ";
cin >> a;
k= 0;
sum = 0;
int* mass = new int[a];
int* mass2 = new int[a];
cout << "Введите массив: ";
i = 0;
for (i = 0; i < a; i++)
{
	cin >> mass[i] >> mass2[i];
	if (mass[i] < 0 && mass2[i] > 0 && sqrt(pow(mass[i], 2) + pow(mass2[i], 2)) > sum)
	{
		sum = sqrt(pow(mass[i], 2) + pow(mass2[i], 2));
		k = i;
	}
}
if (sum > 0) {
	cout << "Точка с координатами (" << mass[k] << ", " << mass2[k] << ") лежит во 2-й четверти на расстоянии " << sum << " от начала координат." << endl;
}
else cout << "(0, 0)" << endl;
