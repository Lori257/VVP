setlocale(LC_ALL, "Russian");
	int m, n, i, j, minPr, pr, minJ;
	cout << "Введите m и n: ";
	cin >> m >> n;
	minPr = INT_MAX;
	pr = 1;
	minJ = 0;
	int** mass = new int* [m];
	for (i = 0; i < m; i++) {
		mass[i] = new int[n];
	}

	cout << "Введите массив: " << endl;
	for (i = 0; i < m; i++) {
		for (j = 0; j < n; j++) {
			cin >> mass[i][j];
		}
	}

	for (j = 0; j < n; j++) {
		pr = 1;
		for (i = 0; i < m; i++) {
			pr = pr * mass[i][j];

		}
		if (pr < minPr) {
			minPr = pr;
			minJ = j;
		}
	}

	cout << "Номер столбца: " << minJ + 1 << endl;
	cout << "Значение наименьшего произведения: " << minPr;
	return 0;
