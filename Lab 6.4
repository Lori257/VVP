setlocale(LC_ALL, "Russian");
	int a, b, c, s1, s2, k, l;
	cout << "Введите стороны а,b,c: ";
	cin >> a >> b >> c;
	s1 = a * b;
	s2 = c * c;
	k = (a / c) * (b / c);
	cout << "количество квадратов, размещенных на прямоугольнике: " << k;
	l = s1 - k * s2;
	cout << "оставшаяся площадь: " << l; 
	return 0;
