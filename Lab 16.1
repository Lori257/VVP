setlocale(LC_ALL, "Russian");
	int n, i, j, k;
	cout << "Введите n: ";
	cin >> n;
	j = 0;
	k = 0;
	int* mass = new int[n];
	for (i = 0; i < n; i++) {
		cin >> mass[i];
	}
	for (i = 0; i < n; i++) {
		if (mass[i] != mass[i + 1]) {
			j = j + 1;
		}
	}
	int* mass2 = new int[j];
	for (i = 0; i < n; i++) {
		if (mass[i] != mass[i + 1]) {
			mass2[k] = mass[i];
			k = k + 1;
		}
	}
	cout << "Новый массив: ";
	for (i = 0; i < j; i++) {
		cout << mass2[i];
	}

	return 0;
