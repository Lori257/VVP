int Sign(float x);

int main()
{
	setlocale(LC_ALL, "Russian");
	cout << "Введите a и b: ";
	float a, b;
	cin >> a >> b;
	cout << Sign(a) + Sign(b);
	return 0;
}

int Sign(float x) {
	if (x < 0) {
		x = -1;
	}
	else if (x == 0) {
		x = 0;
	}
	else x = 1;
	return x;
}
