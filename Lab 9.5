setlocale(LC_ALL, "Russian");
	int n, y, x;
	cout << "Введите год: ";
	cin >> n;
	string col[5] = { "зелено","красно","желто","бело","черно" };
	string anim[12] = { "крысы ", "коровы ", "тигра ", "зайца ", "дракона ", "змеи ",
		"лошади ", "овцы ", "обезьяны ", "курицы ", "собаки ", "свиньи " };
	if (n >= 1984) {
		y = ((n - 1984) % 60) / 12;
		x = ((n - 1984) % 60) % 12;
		if (x == 2 || x == 3 || x == 4) {
			cout << "Год " << col[y] << "го " << anim[x];
		}
		else cout << "Год " << col[y] << "й " << anim[x];
	}
	else {
		y = 4 - ((1983 - n) % 60) / 12;
		x = 11 - ((1983 - n) % 60) % 12;
		if (x == 2 || x == 3 || x == 4) {
			cout << "Год " << col[y] << "го " << anim[x];
		}
		else cout << "Год " << col[y] << "й " << anim[x];
	}
  return 0;
