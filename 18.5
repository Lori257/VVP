setlocale(LC_ALL, "Russian");
	int m, n, i, j, k, c;
	cout << "Введите m и n: ";
	cin >> m >> n;
	k = 0;
	c = n + 1;
	int**mass = new int* [n];
	for (i = 0; i < n; i++) {
		mass[i] = new int[m];
	}
	cout << "Введите элементы матрицы: " << endl;
	for (i = 0; i < n; i++) {
		for (j = 0; j < m; j++)
		{
			cin >> mass[i][j];
		}
	}
	for (j = m - 1; j >= 0; j--)
	{
		for (i = 0; i < n; i++)
		{
			if (mass[i][j] % 2 == 1) {
				k = k + 1;
			}
		}
		if (k == n) {
			c = j;
			k = 0;
		}
	}
	if (c != n + 1)
	{
		cout << "Номер первого столбца, содержащего все нечётные числа:  " << c + 1 << endl;
	}
	else cout << "0" << endl;
	return 0;
